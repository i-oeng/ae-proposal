# Reference Materials

Place approved past proposal excerpts here as Markdown files and add metadata to `reference_index.json`.

The v1 style reference selector uses simple metadata matching only:

1. Same industry and country
2. Same industry
3. Same country
4. No reference

Style references are for tone only. They must not override calculated numbers or the approved facts pack.

